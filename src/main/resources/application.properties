spring.application.name=produits
# ===== CONFIGURATION MYSQL =====
spring.datasource.url=jdbc:mysql://localhost:3306/produitsdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=gestion
spring.datasource.password=gestion123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA / HIBERNATE =====
# update : crée ou modifie les tables automatiquement (sans supprimer les données)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===== PORT DU SERVEUR =====
server.port=8080